# CORS Origins (comma-separated)
# For production, add your frontend URL
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,https://your-app.vercel.app

# Port (optional, defaults to 8000 or $PORT on Railway/Render)
PORT=8000
